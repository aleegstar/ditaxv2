## Änderung

`src/pages/SelectPerson.tsx`, Zeile 48: Wrapper bekommt expliziten weissen Hintergrund.

- Vorher: `className="min-h-screen antialiased relative"` (zeigt globalen warm-off-white Hintergrund durch)
- Nachher: `className="min-h-screen antialiased relative bg-white"`

Keine weiteren Änderungen.